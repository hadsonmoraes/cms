<?php

use Illuminate\Support\Facades\Route;
use {{ namespace }}\{{ module }}\Http\Controllers\{{ module }}Controller;

Route::get('/{{ route }}', [{{ module }}Controller::class, 'index'])
    ->name('{{ namespace }}.{{ module }}.index');
