@extends('sistema::layouts.admin', ['title' => 'Unidades'])

@section('content')
    @if (session('status')) <div class="alert alert-success">{{ session('status') }}</div> @endif
    <div class="row g-3"><div class="col-lg-5"><div class="card"><div class="card-header"><h5 class="mb-0">Nova unidade</h5></div><div class="card-body"><form method="POST" action="{{ route('Sistema.Unidades.store') }}" class="vstack gap-3">@csrf<div><label for="name" class="form-label">Nome</label><input id="name" name="name" class="form-control" value="{{ old('name') }}" required>@error('name')<div class="text-danger small">{{ $message }}</div>@enderror</div><div><label for="code" class="form-label">Código</label><input id="code" name="code" class="form-control" value="{{ old('code') }}" required>@error('code')<div class="text-danger small">{{ $message }}</div>@enderror</div><div><label for="city" class="form-label">Cidade</label><input id="city" name="city" class="form-control" value="{{ old('city') }}"></div><button class="btn btn-primary" type="submit">Cadastrar unidade</button></form></div></div></div><div class="col-lg-7"><div class="card"><div class="card-header"><h5 class="mb-0">Unidades cadastradas</h5></div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Nome</th><th>Código</th><th>Cidade</th></tr></thead><tbody>@forelse ($unidades as $unidade)<tr><td>{{ $unidade->name }}</td><td>{{ $unidade->code }}</td><td>{{ $unidade->city ?? '-' }}</td></tr>@empty<tr><td colspan="3" class="text-muted">Nenhuma unidade cadastrada.</td></tr>@endforelse</tbody></table></div></div></div></div>
@endsection